# Primary domain(s) for the admin MWS instance
PRIMARY_DOMAIN=gyld.app
WWW_DOMAIN=www.gyld.app

# Additional subdomains for other MWS services
SUBDOMAINS="sdm.gyld.app silat.gyld.app"

# Email address for Certbot
SSL_EMAIL="gyld.app@example.com"
CONFIG_FILE="nginx.production.conf"

# Others as needed
